import java.util.Scanner;

class ScanerTest
{
/*	public static void main(String[] args)
	{
		Scanner sc = new Scanner(System.in);
		//Scanner is a class
		//We have created an object for this class Scanner (S is capital)
		sc.close();
		//sc object must be closed after usage, close is a method(in java) or function(in cpp or c)
	}
	*/
	//if we comment main method then we will get RUN TIME error
	//NOT COMPILE TIME, while compiling everything is fine: class is declared correctly with flower brackets and import is also done good
	//but JVM requires a main file to process information
}
